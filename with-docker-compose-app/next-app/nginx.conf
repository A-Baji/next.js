events {
    worker_connections 1024;  # You can adjust the number of worker connections as needed.
}

http {
    # ... other settings ...

    server {
        listen 80;
        server_name localhost;

        location / {
            root /usr/share/nginx/html;
            index index.html;
        }

        # ... other server-specific settings ...
    }

    # ... other server or location blocks ...
}
